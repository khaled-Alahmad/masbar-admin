/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['backend.instahandi.com', 'i.ibb.co', 'masbar-backend.ahmed-albakor.com'],

    }
};

export default nextConfig;
